{% extends "task/base.j2" %}

{% block content %}
{#
    [최적화 버전 v2] 프로젝트 맥락 기반 분석 + Schema 중심 전략

    주요 개선사항:
    1. 프로젝트 맥락 파악 단계 추가 (분석 전 최우선 수행)
    2. 감정 분석 기준 재정립 (프로젝트 가치에 대한 태도 기준)
    3. etc_contents 기준 완화 (단순 참여 의사도 긍정적 관심으로 포함)
    4. Schema description에 제약사항 위임

    공통 모듈:
    - component/content_analysis_requirements.j2: 분석 요구사항 (0~9번)
    - component/merge_results_strategy.j2: 병합 전략

    ---

    # 작업: 포괄적인 콘텐츠 분석 및 분류

    제공된 콘텐츠 아이템들을 분석하고 명확한 분류와 감정 분석을 통해 구조화된 인사이트를 생성합니다.

    ## 입력 데이터 구조
    JSON 배열 형태의 콘텐츠 아이템들
    (input_schema_description 변수로 동적 주입 - 3개 Provider 공통)

    ## 분석 요구사항
    (공통 모듈 _analysis_requirements.j2 참조)

    ## 출력 형식
    제공된 JSON 스키마를 준수하는 유효한 JSON 객체
    **모든 제약사항(최대 개수, 길이 제한)은 스키마에 정의됨**

    ## 이전 분석 결과 (병합 필요) - 조건부 섹션
    (previous_result가 있는 경우)
    (공통 모듈 _merge_strategy.j2 참조)

    **핵심**: 제공된 JSON 스키마의 모든 제약사항을 정확히 준수
#}

# Task: Comprehensive Content Analysis and Categorization

Analyze the provided content items and create structured insights with clear categorization and sentiment analysis.

## Input Data Structure

You will receive a JSON array of content items.

{{ input_schema_description }}

## Analysis Requirements

{% include "task/common/component/content_analysis_requirements.j2" %}

## Output Format

Output must be a valid JSON object adhering to the provided JSON schema.
**All constraints (max counts, length limits) are defined in the schema.**

{% block output_schema_description %}{% endblock %}

{% block json_response_rule %}{% endblock %}

{% if previous_result %}
## Previous Analysis Result (Merge Required)

Below is the analysis result from a previous batch.
You must **MERGE** this with the analysis of new content items to create a **unified result**.

### Merge Strategy

{% include "task/common/component/merge_results_strategy.j2" %}

### Previous Analysis Result
```json
{{ previous_result_json }}
```

---

{% endif %}
## Content to Analyze

Project ID: {{ project_id }}
Project Type: {{ project_type }}
Content Type: {{ content_type }}

Content Items:
```json
{{ content_items }}
```

**CRITICAL**: Follow all constraints defined in the JSON schema exactly.
{% endblock %}
