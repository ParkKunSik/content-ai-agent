# Core Agent & Google Cloud
google-cloud-aiplatform>=1.60.0
google-cloud-storage
google-cloud-secret-manager

# Data Validation & Settings
pydantic>=2.0.0
pydantic-settings

# Infrastructure
python-dotenv
tenacity
jinja2

# Interface (FastAPI is needed if used as a local wrapper in container, but for RE it's handled by runtime)
# We keep fastapi/uvicorn for hybrid cloud run deployment capability
fastapi
uvicorn[standard]